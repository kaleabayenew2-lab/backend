# FindMed Backend

Steps:

1. Create `.env` with at least `MONGO_URI` and optional `PORT`.
2. Install dependencies: `npm install`.
3. Start server: `npm start`.

Routes:
- `GET /api/facilities` — list nearby facilities (to implement)
- `POST /api/facilities` — create facility (admin)

